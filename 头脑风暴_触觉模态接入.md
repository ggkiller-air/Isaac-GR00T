# 头脑风暴:为 SONIC VLA 接入触觉模态(皮肤衣)

> **目标**:在现有 `unitree_g1_sonic`(video + state + language → action)基础上,新增一路上身**触觉/皮肤衣**模态。
> **方案(已锁定)**:参考 HTD 论文,把"**分区触觉 encoder + 触觉 token 注入 + touch dreaming 辅助损失**"
> 三件套**嫁接**进 GR00T 的 Action Head,**不替换** GR00T 预训练的 VLM+DiT。
>
> 参考:*Learning Versatile Humanoid Manipulation with Touch Dreaming (HTD)*,arXiv:2604.13015,CMU+Bosch,2026-04。

---

## 〇、触觉数据现状(已采集:`outputs/carry-bucket-stereo`)

- **磁盘列**:`observation.tactile_raw`,`dtype=uint8`,`shape=[256]`(`raw_001..raw_256`)。
- **传输帧**:256 维 = 前 128 + 后 128 分包合并。
- **有效通道 112/256**,按规格书分 **6 个身体区域**(直接作为 per-region 分区):

  | 区域      | 通道数     |
  | ------- | ------- |
  | 前胸      | 48      |
  | 后背      | 40      |
  | 左臂 / 右臂 | 8 / 8   |
  | 左肩 / 右肩 | 4 / 4   |
  | **合计**  | **112** |

  其余 144 维为空位/保留位。**112 个有效位散布在 256 里,大概率不连续 → 用显式 `valid_idx`(规格书 1-based,代码减 1)做 index-select,不一定能用连续 start/end 切片。**
- **数值**:每通道 1 byte,范围 0–255,是**表征量级**非真实力(真实力另有换算公式)。
- **采集端已做归零 calibration → LeRobot 里静息 = 0**。
- `meta/modality.json` 已登记 `tactile` 段(单 key `tactile_raw`,仅 `original_key`,无切片)。

**归一化决策:走 video 式固定 `/255 → [0,1]`,不走 state 数据集统计量。**
理由:① 112 通道**同质**(同传感器、同单位、同 0–255 字节)→ state 式"逐维标定"的优势不成立;② 静息已归零 → 无基线偏移,`/255` 干净。
**推论:触觉不进 `stats.json`,无需改 `stats.py` / loader 统计逻辑。** 精确力标定留作**对比实验第二臂**。

---

## 一、数据从磁盘到动作的流动路径

模型 = **Backbone(Qwen3-VL,管 video+language)** + **Action Head(DiT flow-matching,管 state+action)**,两路在 Action Head 里通过 cross-attention 融合。

```
磁盘(GR00T-LeRobot 格式)
  observation.state ──(modality.json 切片)──> left_leg/right_leg/... 子键
  observation.tactile_raw[256] ──> 取 112 有效位
  ego_view.mp4
        │
[1] 加载器  lerobot_episode_loader.py
    ALLOWED_MODALITIES = ["video","state","action","language","mask"]  ← 硬编码白名单(要加 tactile)
    _extract_joint_groups 只处理 ["state","action"]                    ← 要加 tactile 分支
        │
[2] 模态配置  embodiment_configs.py: MODALITY_CONFIGS["unitree_g1_sonic"]  ← 加 "tactile" 模态
        │
[3] 处理器  processing_gr00t_n1d7.py
    state 子键 → 归一化 → cat → 补零到 max_state_dim(=132)
    tactile → 取 112 位 → /255                ← 新增,独立于 state,不进 132
        │
[4] 模型  gr00t_n1d7.py: Gr00tN1d7ActionHead
    Backbone: video+language → vl_embeds [B,seq,2048]   ← cross-attn 的“上下文”
    state_encoder  → state_features  [B,1,1536]
    action_encoder → action_features [B,horizon,1536]
    sa_embs = cat(state_features, action_features)       ← 自注意力的“查询流”
    DiT(hidden_states=sa_embs, encoder_hidden_states=vl_embeds) 去噪
    action_decoder(取 sa_embs 尾部 action_horizon 个 token) → 预测动作
```

**决定触觉往哪插的关键结论:**

1. 解码只取 `sa_embs` **尾部** action token(`pred[:, -action_horizon:]`)→ **触觉 token 加在 action_features 之前,绝不影响解码切片**,风险最低。
2. `max_state_dim=132` 只为本体感知 → **触觉独立建模态,不塞进 state**。
3. 触觉是**输入模态**:只需 encoder,**无需后处理/反归一化**(后处理 `decode_action`/`unapply` 只服务 action)。

---

## 二、方案选择(为什么是"HTD 嫁接")

- ❌ **拼进 state 向量**:与本体感知共享 MLP、挤占 132 维,**否决**。
- ❌ **注入 VLM cross-attention 上下文**:改 backbone 输出与 mask,侵入大,**不采用**。
- ❌ **照搬 HTD encoder-decoder trunk**:HTD 从零训练,照搬等于放弃 GR00T/SONIC 预训练,**不采用**。
- ✅ **HTD 三件套嫁接**:保留预训练 VLM+DiT,只移植与宿主无关的三块。改动可控、保住预训练。

> **论文实证(必须重视)**:Fig 6/7——**只把触觉当输入,增益不稳定**;真正稳定增益来自 **touch dreaming 辅助任务**,且 **预测 latent 比预测 raw 更好**。
> → encoder 与 touch dreaming **要一起做**;dreaming 不可省,且做 latent 版。

---

## 三、最终方案:HTD 三件套(具体设计)

### (A) 分区触觉 Encoder(Per-Region)

分区**直接用规格书 6 区**(前胸/后背/左臂/右臂/左肩/右肩):

```
tactile_raw[256] → valid_idx 取 112 位 → /255
  → 按 6 区切分 [48,40,8,8,4,4]
  → 每区独立 MLP 分支(无 2D 空间排布 → 用 MLP,不用 CNN)
  → 投影到 input_embedding_dim(=1536)
  → cross-attention 聚合(可学习 slot query)→ 固定 N 个触觉 token
      tactile_features: [B, N_tactile_tokens, 1536]
```

要点:**每区独立编码再聚合**(保区域语义);token 数 N 由 query 控制、与原始维度解耦。

### (B) 触觉 token 注入 sa_embs

```python
# 现状:
sa_embs = torch.cat((state_features, action_features), dim=1)
# 改为:
sa_embs = torch.cat((state_features, tactile_features, action_features), dim=1)
```

放在 `action_features` 之前,不影响尾部解码切片。
**⚠️ 两处都要改:训练 `forward`(~~234)+ 推理 `get_action_with_features`(~~399),否则训练/推理序列结构不一致。**

### (C) Touch Dreaming 辅助损失(关键增益,不可省)

Action Head 加 **dream head + EMA target encoder**,训练时预测**未来触觉 latent**:

- **目标(EMA teacher,Eq.8)**:`z*_{t+k} = stopgrad( T_ema(未来触觉) )`;`θ_ema ← α·θ_ema + (1-α)·θ`(Eq.4),不回传梯度。
- **预测**:dream head 从主干特征预测 `ẑ_{t+k}`,k=1..τ。
- **损失(Eq.9)**:`L_tact = mean[ (1-cos(ẑ,z*)) + β·smoothL1(‖ẑ‖-‖z*‖) ]`(方向项 + 幅度项,幅度项防塌缩)。
- **总损失**:`L = L_action(flow-matching) + λ_Z·L_tact`。
- dream head **仅训练用,推理不跑**,不增加部署开销。

### (D) token 流转全程(形状 + 双 head)

**第一步:各模态编码,宽度统一到 1536,token 数各异**(设 B=batch,历史=1,N=8)

| 模态 | encode 前 | encoder | encode 后 | token 数 L | 含义 |
|---|---|---|---|---|---|
| state | `[B,132]`(43维cat补零到132) | state_encoder(MLP) | `[B,1,1536]` | 1 | 当前快照(压扁) |
| tactile | `[B,112]`(取112位/255) | tactile_encoder(6区MLP+聚合) | `[B,N,1536]` | N=8 | 当前快照(**空间/语义轴**) |
| action | `[B,40,act_dim]`(40步动作块) | action_encoder | `[B,40,1536]` | 40 | 未来展开(**时间轴**) |

> **关键洞察**:encoder 的使命是把各异的宽度(132/112/act_dim)统一成 1536;token 数 L 各模态自定。
> **action 的 40 = 时间轴**(未来 40 帧,有时序);**tactile 的 N = 空间/语义轴**(同一时刻触觉的 N 个聚合槽,无时序);**state 同属"当前快照",只是压成 1 个 token**。
> 三者摆在同一 `dim=1` 上,self-attn 不分意义一视同仁 → **动作 token(时间)能 attend 触觉 token(空间)**,模型自学"前胸压力→接下来动作怎么调"的时空因果。这正是选"混进 sa_embs"而非另开 cross-attn 的理由(双向自由关联)。
> tactile 内部:`[B,112]`→按6区切→6个区域MLP→`[B,6,1536]`→N个可学习query做cross-attn聚合→`[B,N,1536]`。**112(原始维)、6(区域数)、N(token数)三者解耦**。

**第二步:拼接 + DiT + 双 head 解码**

```
state_features  [B,1,1536] ┐
tactile_features[B,N,1536] ├ cat(dim=1) → sa_embs [B,1+N+40,1536]
action_features [B,40,1536]┘
        ↓ DiT(self-attn 互相关联 + cross-attn 吃 vl_embeds)
   model_output [B,1+N+40,hidden]
        ├─ action_decoder → pred[:, -40:] → 未来40步动作      (训练+推理)
        └─ dream_head     → 未来τ步触觉 latent ẑ              (仅训练)
                                 ↑ 对比 EMA teacher 编码的 z*,算 L_tact
```

- 解码从**尾部**锚定(`pred[:, -40:]`)→ state/tactile token 在前段,**只作上下文、被丢弃**,不影响动作切片。
- **dreaming 的目的**:逼主干**从当前观测+动作预判未来触觉后果** → 把"动作-触觉因果"编码进共享表征,受益的是整个 trunk(进而 action 预测)。这是论文唯一稳定增益来源:光当输入会被忽略,逼它预测才真正用起来。
- 推理只跑 action_decoder,dream_head 丢弃(预测的触觉不能驱动机器人)→ 零部署开销。

---

## 四、逐文件改动清单


| #   | 文件                                          | 改动                                                                                                                                            |
| --- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `data/dataset/lerobot_episode_loader.py`    | `ALLOWED_MODALITIES` 加 `"tactile"`;`_extract_joint_groups` 加 tactile 分支(取 112 位)                                                              |
| 2   | `data/types.py`                             | `VLAStepData` 加 `tactile` 字段                                                                                                                  |
| 3   | `configs/data/embodiment_configs.py`        | `unitree_g1_sonic` 加 `"tactile": ModalityConfig(delta_indices=[0]+未来τ步, modality_keys=[6区])`                                                  |
| 4   | `model/gr00t_n1d7/processing_gr00t_n1d7.py` | 构建 tactile 张量(取112位 + /255);打进输出 dict;`process_observation` 同步                                                                                |
| 5   | `configs/model/gr00t_n1d7.py`               | 加 `use_tactile`、6 区维度、`N_tactile_tokens`、`dream_horizon τ`、`ema_decay α`、`lambda_tactile`                                                     |
| 6   | `model/modules/`(新文件)                       | (A) per-region tactile encoder + cross-attn 聚合(类定义在此,`gr00t_n1d7.py` import)                                                                  |
| 7   | `model/gr00t_n1d7/gr00t_n1d7.py`            | `__init__` 注册 `tactile_encoder`/EMA teacher/dream head;(B) 改两处 `sa_embs`;`forward` 加 (C) `L_tact`;`set_trainable_parameters` 加 `tune_tactile` |
| 8   | `Gr00tN1d7DataCollator`(同 processing 文件)    | **几乎不用改**(通配 `np.stack` 自动覆盖 tactile;保证定长可 stack 即可)                                                                                          |


> **不需要改**:`stats.py` / loader 的 `get_dataset_statistics`(触觉走 /255,不进统计量)。

---

## 五、训练策略 + 形状速记

**训练策略**

1. 冻结 backbone(`tune_llm/tune_visual=False`),必要时也冻 diffusion,**先只训** tactile encoder + projector + dream head 让新模块收敛。
2. EMA teacher 用 student tactile encoder 初始化,`α` 接近 1(慢更新)。
3. 先确认 `L_tact` 正常下降、不塌缩(幅度项是否生效),再放开联合微调。
4. 触觉 `delta_indices` 含未来帧(touch dreaming 监督),改它**不影响 stats**(触觉不进统计量)。

**形状速记**

- `input_embedding_dim=1536`(触觉 token 对齐);`max_state_dim=132`(本体感知用,触觉不走)。
- `state_features [B,1,1536]` → 触觉 `[B,N,1536]` → 拼进 `sa_embs`。
- 触觉 6 区维度:`[48,40,8,8,4,4]`,合计 112(有效)/ 256(原始包)。

---

## 六、关键文件速查表(定位用)


| 作用                                | 文件                                                                                            |
| --------------------------------- | --------------------------------------------------------------------------------------------- |
| 模态白名单 / 列映射 / VLAStepData 组装      | `data/dataset/lerobot_episode_loader.py`(`ALLOWED_MODALITIES`:59;`_extract_joint_groups`:303) |
| 数据结构 VLAStepData / ModalityConfig | `data/types.py`(`VLAStepData`:50;`ModalityConfig`:84)                                         |
| SONIC 模态 key 定义                   | `configs/data/embodiment_configs.py`(`unitree_g1_sonic`:67–114)                               |
| 触觉前处理(取112位+/255)                 | `model/gr00t_n1d7/processing_gr00t_n1d7.py`(`process_observation`:374;`__call__`:510)         |
| 模型维度/开关                           | `configs/model/gr00t_n1d7.py`                                                                 |
| Action Head(sa_embs 拼接两处)         | `model/gr00t_n1d7/gr00t_n1d7.py`(训练 `forward`:234;推理 `get_action_with_features`:399)          |
| encoder 写法参考(CategorySpecificMLP) | `model/modules/embodiment_conditioned_mlp.py`(:143)                                           |
| 自动补算统计量(训练入口调用)                   | `data/dataset/factory.py`(:59/62)→ `data/stats.py`                                            |
| 参考论文 PDF                          | `2604.13015v2.pdf`(HTD)                                                                       |


