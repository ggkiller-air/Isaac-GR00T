# 触觉模态接入 · 实施计划（基于代码核实后的可执行版）

> 配套头脑风暴：`头脑风暴_触觉模态接入.md`。本文件是**核实代码现状后**的落地计划，供 review。
> 数据集：`outputs/carry-bucket-stereo`（62 episode / 106441 帧 / 50fps / `observation.tactile_raw` uint8[256]）。
> 验收口径（已与你确认）：**能端到端启动训练 + 触觉前向/反向跑通 + 形状/梯度无误**即可；loss 收敛你自己 tmux 看。

---

## ✅ 实施状态（已落地 + 测试）

- **代码已全部实现**（数据层 4 文件 + 触觉模块新文件 + 模型 3 处 + config + 启动接线）。
- **13 个 CPU 测试全过**：`tests/test_tactile_data_path.py`（真实数据集验证）、`tests/test_tactile_encoder.py`、`tests/test_tactile_action_head.py`（真实 DiT 前向/反向）。
- **启动用现成脚本**：`examples/sonic_tactile/finetune_sonic_tactile.sh`；说明见 `examples/sonic_tactile/README.md`。
- **规格书映射已接入**：`gr00t/data/tactile_layout.py`，真实区序/大小 = `[front_chest 48, back 40, left_arm 8, left_shoulder 4, right_arm 8, right_shoulder 4]` = `[48,40,8,4,8,4]`（注意非 doc 早先写的 `[48,40,8,8,4,4]`），112 位全部覆盖数据中曾激活的 89 通道。
- **两个待你确认/操作项**：① stereo 用左/右/双相机（默认单路 `ego_view_left`，见 modality_config.py 一行切换）；② `HF_TOKEN` 拉 `nvidia/GR00T-N1.7-3B`（~6GB，非 gated）或给本地 SONIC 检查点。

---

## 0. 已锁定的决策 & 你需要提供的东西

| 项 | 结论 |
|---|---|
| **触觉分区** | 按规格书 **6 物理区 [48,40,8,8,4,4]=112** 建 encoder。`valid_idx`(256→112) + 每通道区域归属做成 **config**，**等你提供映射**填入。 |
| **回退（映射到位前）** | `valid_idx=None` 时自动退化为「全 256 单区 + dreaming」，保证映射没到也能先跑通；映射到位后改一行 config 升级为 6 区。 |
| **归一化** | 触觉走 `/255 → [0,1]`，**不进 `stats.json`**，不改 `stats.py`。 |
| **dreaming** | 预测**未来触觉 latent**（HTD 实证比 raw 好），EMA teacher + 幅度项防塌缩。**不可省**。 |
| **验证强度** | 只做「runnable」级：1~2 个真实 batch 跑通 forward/backward + 短启动 sanity。不做收敛实验。 |
| **Base 权重** | 机器上**无** `GR00T-N1.7-3B`（只有 Cosmos backbone + LIBERO 微调权重）。需 ~6GB 下载，NVIDIA EA 仓库基本要 `HF_TOKEN`+网页接受许可；**或**你给本地 SONIC 检查点路径免下载。 |

**🔑 我需要你给的 2 样东西（不给也能先用回退跑通）：**
1. **`tactile_valid_idx`**：规格书里 112 个有效位在 256 中的位置（1-based 我会 -1），以及它们按 6 区的顺序 [前胸48, 后背40, 左臂8, 右臂8, 左肩4, 右肩4]。
2. **`--base-model-path`**：SONIC 基座检查点路径，或确认用 `nvidia/GR00T-N1.7-3B`（我配 HF_TOKEN 下载）。

---

## 1. 代码核实结论（含对头脑风暴 doc 的 3 处修正）

✅ doc 里的落点全部真实存在，行号高度吻合（forward 拼接 234、尾部解码 259、推理拼接 399、解码 418）。

**修正/补充（doc 没写对/漏写的）：**

1. **⚠️ 漏了一个关键文件 `data/dataset/sharded_single_step_dataset.py`。**
   doc 把数据改动都归到 `lerobot_episode_loader.py`，但**真正的 delta_indices 时序窗口化 + VLAStepData 组装**在 `sharded_single_step_dataset.py:30-80`。
   - 好消息：该函数**对模态通用**（`for modality, config in modality_configs.items()` 遍历），但：
     - 数值化分支 `if modality in ["state","action"]:`（:51）需加 `"tactile"` → 才会 `np.vstack` 成数值数组而非 list。
     - VLAStepData 组装（:64-79）字段是**硬编码**的，要加 `tactile=step_data.get("tactile", {})`。
   - touch dreaming 的「未来 τ 帧触觉」就靠这里的 `delta_indices=[0,1,...,τ]` 自动取到，**无需新写时序逻辑**。

2. **`model` 侧 `prepare_input` 不用改。** 它是 `return BatchFeature(data=batch)`（:471-473）纯透传——只要 collated batch 里有 `tactile` 键，`action_input.tactile` 自动可用。

3. **DataCollator 不用改 & 6 区切分不在 modality.json。**
   - Collator（:104-139）对非特殊键泛型 `torch.from_numpy(np.stack(values))`，触觉只要是定长 numpy 自动覆盖。✅ doc 判断正确。
   - modality.json 里 tactile 只有单 key `tactile_raw`（无 start/end），所以 **6 区切分放在 encoder 内**，modality.json 不动；loader 的 `_extract_joint_groups` 会因取 `["start"]` 崩，故 tactile 要**单独整列加载**（见下）。

---

## 2. 逐文件改动清单（确切锚点）

| # | 文件 | 改动 | 锚点 |
|---|---|---|---|
| 1 | `gr00t/data/dataset/lerobot_episode_loader.py` | `ALLOWED_MODALITIES` 加 `"tactile"`；`_load_parquet_data` 末尾加 tactile 整列加载 `loaded_df["tactile.tactile_raw"]=original_df["observation.tactile_raw"]`（**不走** `_extract_joint_groups`） | `:59` / `:385-396` |
| 2 | `gr00t/data/dataset/sharded_single_step_dataset.py` | 数值化分支加 `"tactile"`；VLAStepData 加 `tactile=` 字段映射 | `:51` / `:64-79` |
| 3 | `gr00t/data/types.py` | `VLAStepData` 加 `tactile: dict[str, np.ndarray] | None = None` | `:51` |
| 4 | `gr00t/configs/data/embodiment_configs.py` | `unitree_g1_sonic` 加 `"tactile": ModalityConfig(delta_indices=[0,1,...,τ], modality_keys=["tactile_raw"])` | `:67-114` |
| 5 | `gr00t/configs/model/gr00t_n1d7.py` | 加 config 字段（见 §3.4） | `:122` 后 |
| 6 | `gr00t/model/modules/tactile_encoder.py`（**新**） | per-region encoder + slot 聚合 + dream head + EMA teacher | 新文件 |
| 7 | `gr00t/model/gr00t_n1d7/processing_gr00t_n1d7.py` | 训练 `__call__` 和推理 `process_observation` 各加触觉张量构建（valid_idx select + /255）；写入输出 dict。Collator/`__init__` 仅加少量参数透传 | `:510-625` / `:374-414` |
| 8 | `gr00t/model/gr00t_n1d7/gr00t_n1d7.py` | `__init__` 注册触觉模块（`use_tactile` 门控）；`forward` 两件事：拼 `sa_embs`(:234)+算 `L_tact`(:264 后)；`get_action_with_features` 拼 `sa_embs`(:399)；`set_trainable_parameters` 加 `tune_tactile` | 见锚点 |

**不改**：`stats.py`、`factory.py` stats 入口、`DataCollator`、`prepare_input`、modality.json。

---

## 3. 触觉模块设计

### 3.1 数据流形状（训练，B=batch，τ=dream_horizon，N=N_tactile_tokens）

```
parquet observation.tactile_raw uint8[256]
  └─[loader] tactile.tactile_raw 整列
     └─[sharded_single_step_dataset] delta_indices=[0..τ] 取窗口 → vstack
        → VLAStepData.tactile["tactile_raw"]  float32[τ+1, 256]
           └─[processor] valid_idx 取112 (回退:全256) → /255
              → inputs["tactile"]  float32[τ+1, 112]
                 └─[collator np.stack] → batch  [B, τ+1, 112]
                    └─[action head]
                        cur = tactile[:, 0]          [B,112]
                          → 6区MLP → [B,6,1536] → N slot cross-attn
                          → tactile_features [B,N,1536]   ──┐ 进 sa_embs
                        future = tactile[:, 1:]      [B,τ,112]
                          → EMA teacher(no grad) → z*  [B,τ,d_z]  ← dreaming 目标
```

### 3.2 (A) 分区 encoder（`tactile_encoder.py`）

```
PerRegionTactileEncoder:
  cur[B,112] → split [48,40,8,8,4,4]（回退: 整段[256]单区）
    → 每区独立 2层MLP → 投影到 1536 → [B, 6, 1536]
SlotAggregator:
  N 个可学习 query → cross-attn(query=slots, key/value=区域token) → [B, N, 1536]
```
- 用 MLP 非 CNN（无 2D 空间排布）。区域数(6) / 原始维(112) / token数(N) 三者解耦。
- **非** CategorySpecific（触觉只此一个 embodiment），普通 `nn.Linear`，更干净。

### 3.3 (C) Touch Dreaming（关键增益）

```
EMA teacher  = student encoder 的 EMA 副本 → 池化出 latent z*  [B,τ,d_z]（stopgrad）
  θ_ema ← α·θ_ema + (1-α)·θ   （forward 内 @no_grad 更新，仅 training）
dream head   = 从 DiT 输出的触觉 token 段池化 → MLP → ẑ  [B,τ,d_z]
L_tact = mean_k[ (1 - cos(ẑ_k, z*_k)) + β·smoothL1(‖ẑ_k‖ - ‖z*_k‖) ]   # 方向 + 幅度(防塌缩)
L = L_action(flow-matching) + λ_Z · L_tact
```
- dream head **仅训练**，推理丢弃 → 零部署开销。
- d_z 默认 = 1536（池化后）；α 默认 0.99；β 默认 1.0；λ_Z 默认 0.5（你 tmux 里可调）。

### 3.4 config 新增字段（`gr00t_n1d7.py`）

```python
use_tactile: bool = False              # 总开关，不开则完全不影响原模型
tactile_raw_dim: int = 256
tactile_valid_idx: list[int] | None = None   # 你提供；None→用全256回退
tactile_region_sizes: list[int] = [48,40,8,8,4,4]   # 回退时自动设 [256]
n_tactile_tokens: int = 8
dream_horizon: int = 4                  # τ：未来帧数（50fps，4≈80ms）
tactile_latent_dim: int = 1536
ema_decay: float = 0.99
lambda_tactile: float = 0.5
tactile_dream_beta: float = 1.0
tune_tactile: bool = True
```

### 3.5 sa_embs 注入（两处都改，保持训练/推理结构一致）

```python
# forward:234  /  get_action_with_features:399
sa_embs = torch.cat((state_features, tactile_features, action_features), dim=1)
#                                     ^^^^^^^^^^^^^^^^ 加在 action 之前
```
解码仍 `pred[:, -action_horizon:]`（:259/:418）——触觉 token 在前段只作上下文、被丢弃，**不影响动作切片**。推理无未来帧 → 只用 cur 编码、dream head 不跑。

---

## 4. 执行批次（开 auto 后按此推进，每批跑通再进下一批）

### Phase 1 — 数据层（改动 #1~#4）
产出：dataset 能吐出带 `tactile` 的 batch。
**runnable 验收**：写个 5 行脚本，用 `unitree_g1_sonic` + 本数据构 dataset，取 1 个 batch，断言 `batch["tactile"].shape == [B, τ+1, 112]`（回退 256）、dtype float、无 NaN。

### Phase 2 — 触觉模块（改动 #5、#6、#7 处理器）
产出：`tactile_encoder.py` 单测（随机张量进，形状对）；processor 产出 `inputs["tactile"]`。
**runnable 验收**：`PerRegionTactileEncoder`+`SlotAggregator` 输入 `[2,112]`→输出 `[2,N,1536]`；EMA teacher 输出 `[2,τ,d_z]`；dream head 形状对、cos/smoothL1 可算。

### Phase 3 — 接进 Action Head + 启动训练（改动 #8）
产出：`use_tactile=True` 下 `forward` 返回含 `loss`(=action+λ·L_tact) 和 `tactile_loss`；两处 sa_embs 改好。
**runnable 验收**：
1. 单 batch `forward`+`loss.backward()` 跑通，触觉模块梯度非零、原模块形状不变。
2. `use_tactile=False` 时与改前**完全等价**（回归保护）。
3. `bash examples/finetune.sh ... --embodiment-tag unitree_g1_sonic`（`MAX_STEPS=5, NUM_GPUS=1, USE_WANDB=0`）真启动、能迭代几步不报错、能存 checkpoint。

> 训练策略（写进示例命令，给你 tmux 用）：先 `tune_llm=tune_visual=False`、可选冻 diffusion，只放开触觉模块 + projector，确认 `L_tact` 动起来不塌缩；再放开联合微调。

---

## 5. 风险 & 回退

| 风险 | 处理 |
|---|---|
| valid_idx 映射未到 | `use_tactile=True` + `valid_idx=None` → 全 256 单区回退，照常 dreaming，先跑通 |
| 推理端未接触觉 key | `process_observation` 里 tactile 缺失时走零张量/跳过，不阻断既有 SONIC 推理 |
| `use_tactile=False` 回归 | 所有触觉代码 config 门控，默认 False，老路径零改动 |
| EMA teacher 显存 | teacher 仅 encoder 体量，`@no_grad`，开销小 |
| base 权重下载 | 你给本地路径免下载；或配 HF_TOKEN |

---

## 6. 一句话总结
数据层 4 文件（含 doc 漏掉的 `sharded_single_step_dataset.py`）让 batch 带上 `[B,τ+1,112]` 触觉；模型层新增 `tactile_encoder.py` + 在 `gr00t_n1d7.py` 两处拼 `sa_embs`、一处加 `L_tact`；全程 `use_tactile` 门控、`/255` 不进 stats、解码尾部锚定不受影响。目标：**开起来能训、触觉真前向反向参与**。
